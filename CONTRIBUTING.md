# 贡献指南

欢迎反馈三星平板的兼容性、动画表现、功耗和可访问性问题。请提供可复现步骤、型号、Android / One UI 版本和键盘名称。应用的兼容性报告不含唯一设备标识；粘贴前仍请自行检查。

不要提交个人通知、账号、序列号、完整系统日志、密钥、签名文件或密码。不要为了排查问题关闭锁屏安全、温控或安装来源保护。

提交代码前运行 `testDebugUnitTest`、`assembleDebug` 和 `lintDebug`。针对传感器门槛、场景隔离、截图生命周期和同意状态的变更应附有相关验证。不要将未实测的型号标注为已验证。

你的贡献应当是你有权按本项目 MIT 许可证提供的内容；新增第三方内容须明确来源、许可证和署名。请保持无联网、用户自愿启用及可随时停用的设计，涉及新数据处理或权限时同步更新风险说明及同意版本。
